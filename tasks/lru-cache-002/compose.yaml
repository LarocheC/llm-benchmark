services:
  default:
    build:
      context: .
      dockerfile: Dockerfile
    command: "tail -f /dev/null"
    working_dir: /workspace
    init: true
    network_mode: none      # hermetic: deps are baked into the image at build time
    stop_grace_period: 1s
